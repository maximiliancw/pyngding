[project]
name = "pyngding"
version = "0.1.0"
description = "A lightweight LAN presence scanner and dashboard for network administrators"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "bottle>=0.12.0",
]

[project.scripts]
pyngding = "pyngding.main:cli"
